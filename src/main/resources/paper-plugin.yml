name: Skylight
version: '${project.version}'
main: uk.firedev.skylight.Skylight
api-version: '1.21'
author: FireML
dependencies:
  server:
    Vault:
      required: true
      load: BEFORE
    DaisyLib:
      required: true
      load: BEFORE
    CMI:
      required: false
      load: BEFORE
    CMILib:
      required: false
      load: BEFORE
    PlaceholderAPI:
      required: false
      load: BEFORE
    MiniPlaceholders:
      required: false
      load: BEFORE