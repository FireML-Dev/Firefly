name: Firefly
version: '${project.version}'
main: uk.firedev.firefly.Firefly
api-version: '1.21'
author: FireML
dependencies:
  server:
    Vault:
      required: true
      load: BEFORE
    DaisyLib:
      required: true
      load: BEFORE
    CMI:
      required: false
      load: BEFORE
    PlaceholderAPI:
      required: false
      load: BEFORE
    MiniPlaceholders:
      required: false
      load: BEFORE