# Here you can add custom commands, these commands can currently execute other commands and/or send messages.

example:
  permission: "example.permission.1"
  aliases:
    - "exa"
  commands:
    - "command1"
    - "command2"
  messages:
    - "<aqua>This is a message example"
    - "<light_purple>And this is the next line :)"